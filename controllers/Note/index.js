const newNote = require('./newNote');
const titleNote = require('./titleNote');
const modifyNote = require('./modifyNote');
const categoryNote = require('./categoyNote');
const deleteNote = require('./deleteNote');

module.exports = {
    newNote,
    titleNote,
    modifyNote,
    categoryNote,
    deleteNote,
};
